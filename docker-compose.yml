services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ~/.ssh:/home/tty/.ssh:ro
    environment:
      - PORT=3000
      - HOST=0.0.0.0
      - DATA_DIR=/app/data
      - CLIENT_DIST=/app/client/dist
    restart: unless-stopped
